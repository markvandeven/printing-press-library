{
  "schema_version": 1,
  "applied_at": "2026-05-13",
  "base_run_id": "20260512-170855",
  "base_printing_press_version": "4.5.0",
  "patches": [
    {
      "id": "freshservice-domain-and-auth",
      "summary": "Substitute FRESHSERVICE_DOMAIN into BaseURL and switch HTTP Basic to `apikey:` (empty password). Added normalizeFreshserviceDomain helper to accept the lenient pasted forms users actually have (bare slug, https://, /api/v2/, etc.).",
      "reason": "Generator defaulted BaseURL to the placeholder `https://yourcompany.freshservice.com/api/v2` with no env-var substitution, so every API call DNS-failed. The generated AuthHeader() also assembled Basic credentials as `apikey:domain`; Freshservice's Basic scheme is `apikey:` (empty password) and rejects the alternative form. Discovered live, fixed in printed CLI, then filed against the generator.",
      "files": [
        "internal/config/config.go"
      ],
      "validated_outcome": "Live read smoke (tickets get/list, agents list, assets list, sync) succeeded against the real tenant after this fix; previously every request returned a transport-level DNS error or HTTP 401."
    },
    {
      "id": "freshservice-filter-query-quoting",
      "summary": "Wrap the value of `query=` in literal double quotes before sending /tickets/filter and /changes/filter requests, and route both filter commands through the wrapFreshserviceFilterQuery helper.",
      "reason": "Freshservice's filter endpoint requires the entire query value to arrive as a double-quoted string before URL encoding; the generator emitted the raw user string and the API responded HTTP 500. Same quirk applies to /assets?filter=, so the resolver in assets_get.go calls the same helper.",
      "files": [
        "internal/cli/freshservice_novel.go",
        "internal/cli/tickets_filter.go",
        "internal/cli/changes_filter.go"
      ],
      "validated_outcome": "tickets filter --query \"status:2 AND priority:4\" now returns the expected page; previously returned `{\"description\":\"Validation failed\",\"errors\":[{\"field\":\"query\",\"message\":\"There was an unexpected error\"}]}` with HTTP 500."
    },
    {
      "id": "freshservice-page-pagination",
      "summary": "Change sync's cursorParam default from `after` to `page`, and synthesize the next integer page number whenever a full page came back without a body cursor.",
      "reason": "Freshservice paginates exclusively by integer `?page=N` and exposes the next-page URL only in the HTTP Link response header. extractPageItems' body-cursor scan therefore never finds a cursor, and the sync loop stops after page 1. The generator's pagination-style detection picked `after` from the spec without checking that Freshservice declares no body cursor.",
      "files": [
        "internal/cli/sync.go"
      ],
      "validated_outcome": "`freshservice-pp-cli sync --resource tickets` paginates through all pages until items < per_page returns; previously stopped at 100 tickets even when the tenant had thousands."
    },
    {
      "id": "freshservice-doctor-tenant-shape-and-probe",
      "summary": "Doctor now (a) flags a .myfreshworks.com BaseURL as the Freshworks unified-org dashboard, not the Freshservice API tenant, and (b) issues an authenticated GET /agents/me probe with explicit branches for 401/403/404 and for the dashboard's HTML-200 response.",
      "reason": "Users routinely paste their org dashboard subdomain (e.g. `<org>.myfreshworks.com`) when prompted for FRESHSERVICE_DOMAIN. That host returns the dashboard SPA's HTML with HTTP 200 for every API path, which the generator's reachability-only doctor treated as a successful API call — every downstream command then failed with confusing JSON-parse errors instead of an obvious 'wrong host' verdict.",
      "files": [
        "internal/cli/doctor.go"
      ],
      "validated_outcome": "doctor now prints `domain_hint: FAIL: ... looks like the Freshworks unified-org dashboard` and `credentials: FAIL: /agents/me returned HTML, not JSON` when the wrong subdomain is configured; previously printed `api: reachable` and `credentials: OK`."
    },
    {
      "id": "freshservice-assets-display-id-resolver",
      "summary": "Accept asset name or asset_tag as the GET /assets/{display_id} argument by issuing a one-shot /assets?filter=name:'…' (or asset_tag:'…') resolver before the canonical GET fires.",
      "reason": "The OpenAPI path parameter is the numeric display_id, but the identifiers humans and agents actually have are name and asset_tag. The generator emitted a path-only command that returned 404 on every human-readable input.",
      "files": [
        "internal/cli/assets_get.go"
      ],
      "validated_outcome": "`assets get LAPTOP-001` resolves the asset_tag to its display_id and returns the asset record; previously returned HTTP 404."
    },
    {
      "id": "freshservice-notes-create-private-default",
      "summary": "Always send `body[\"private\"]` in tickets notes create, even when the flag is the zero value (false), and clarify the flag description.",
      "reason": "The generator's 'omit optional bool when its zero-value matches the spec default' idiom interacts badly with Freshservice's server-side default of `private: true` on this endpoint. The CLI default of --private=false matches what a human typing `tickets note <id>` virtually always intends (public reply to the requester), but omitting the field shipped the note as private. Heads-up was caught during live write testing.",
      "files": [
        "internal/cli/tickets_notes_create-ticket.go"
      ],
      "validated_outcome": "`tickets notes create-ticket <id> --body \"…\"` (no --private flag) posts a public reply visible to the requester; previously posted as agent-only private."
    },
    {
      "id": "freshservice-novel-commands",
      "summary": "Hand-authored novel command set under freshservice_novel.go (breach-risk, my-queue, workload, change-collisions, recurrence, kb-gaps, orphan-assets, dept-sla, oncall-gap) and a tenant-specific code→label resolver in freshservice_meta.go backed by the synced ticket-form-fields choices.",
      "reason": "These transcendence commands aggregate across resources (tickets + agents + groups + assets + changes + KB articles) and rely on FTS scoring against the synced local store; the OpenAPI spec describes the underlying endpoints but not the aggregation. Per-tenant integer codes for status/priority/type/source/urgency/impact mean a static enum-to-label map (what the generator emits) is wrong on every Freshservice tenant — the resolver synthesizes the map from synced choices at query time, with built-in fallbacks for the canonical 2/3/4/5 codes when the local store has not been hydrated yet.",
      "files": [
        "internal/cli/freshservice_novel.go",
        "internal/cli/freshservice_meta.go"
      ],
      "validated_outcome": "All 9 novel commands return non-empty results against the live tenant; `tickets get` and `tickets list` decorate status/priority codes with the tenant's actual labels (e.g. custom status code 9 → 'Awaiting Customer Response') rather than blank or built-in defaults."
    },
    {
      "id": "freshservice-notes-delete",
      "summary": "New `tickets notes delete <conversation-id>` command wired into the notes subcommand tree. Hand-authored DELETE /conversations/{id} request with --dry-run / --yes consent gating and a JSON success envelope.",
      "reason": "Freshservice documents DELETE /conversations/{id} but does not declare it in the OpenAPI spec we generated from, so the generator never emitted a delete-note command. Notes are stored as conversations and the conversation id alone identifies the note (no /tickets/{ticket_id}/ segment).",
      "files": [
        "internal/cli/tickets_notes_delete.go",
        "internal/cli/tickets_notes.go"
      ],
      "validated_outcome": "`tickets notes delete <id> --yes` returns HTTP 204 and the note disappears from `tickets conversations list-ticket <ticket-id>`; previously there was no CLI surface for note deletion."
    },
    {
      "id": "freshservice-logout-clears-apikey",
      "summary": "Extend ClearTokens to also zero FreshserviceApikey and AuthHeaderVal so `auth logout` actually drops the credential under Freshservice's API-key/Basic auth model.",
      "reason": "Greptile P1 on PR #533. Generator's ClearTokens is OAuth-shaped — it clears AccessToken/RefreshToken/TokenExpiry but leaves the API-key fields intact. Because AuthHeader() rebuilds the Basic credential from FreshserviceApikey, every command run after `auth logout` continued to authenticate successfully.",
      "files": [
        "internal/config/config.go"
      ],
      "validated_outcome": "`auth logout` followed by any authenticated command now fails with 'no credentials configured' as expected; previously the command succeeded as if logout had never happened."
    },
    {
      "id": "freshservice-resolvebyname-field-guard",
      "summary": "Add the same validIdentifierRE.MatchString(field) guard ListField uses, so ResolveByName cannot fmt.Sprintf an unvalidated field name into the json_extract SQL path.",
      "reason": "Greptile P1 (security) on PR #533. ListField and ResolveByName were intended to mirror each other (the in-file comment at store.go:1966 states this), but the generator emitted the validIdentifierRE guard only on ListField. Today's callers all pass literal identifiers like \"name\"/\"email\", so blast radius is bounded — but the asymmetry is the real smell and the guard belongs there as defense in depth.",
      "files": [
        "internal/store/store.go"
      ],
      "validated_outcome": "ResolveByName now silently skips any matchFields value that does not look like a SQL identifier; legitimate callers (passing \"name\"/\"key\"/\"email\") behave identically."
    },
    {
      "id": "freshservice-analytics-cmd-outorstdout",
      "summary": "Route analytics JSON and text output through cmd.OutOrStdout() instead of os.Stdout in both the summary path and runGroupBy. Threaded *cobra.Command into runGroupBy so it has the writer available.",
      "reason": "Greptile P2 on PR #533. Direct os.Stdout writes bypass both test redirection (`cmd.SetOut(buf)` in unit tests) and the MCP shell-out layer that captures CLI output. Every other command in the package routes through cmd.OutOrStdout(); analytics was the lone exception.",
      "files": [
        "internal/cli/analytics.go"
      ],
      "validated_outcome": "`analytics --json` output is now capturable via `cmd.SetOut(buf)` in tests and is correctly proxied through MCP; previously the writer-redirection was silently ignored."
    },
    {
      "id": "freshservice-auth-hint-domain-placeholder",
      "summary": "Show a real Freshservice subdomain example for FRESHSERVICE_DOMAIN (acme.freshservice.com) instead of the generator's token-shaped \"your-token-here\" placeholder. Clarify that the value is the tenant subdomain, not the Freshworks org dashboard.",
      "reason": "Greptile P2 on PR #533. The generator emits the same token placeholder for every required env var, but FRESHSERVICE_DOMAIN is a subdomain. A user copying the hint verbatim ends up either with a literal \"your-token-here\" host (DNS-fails) or guesses at <org>.myfreshworks.com (the Freshworks unified-org dashboard) — exactly the trap that the doctor command was built to diagnose. The hint should not be the on-ramp to the trap.",
      "files": [
        "internal/cli/auth.go"
      ],
      "validated_outcome": "`auth status` for an unconfigured CLI now prints `export FRESHSERVICE_DOMAIN=\"acme.freshservice.com\"` with an inline note about the tenant-vs-dashboard distinction; previously printed the misleading \"your-token-here\" form."
    }
  ],
  "deferred_to_upstream": [
    {
      "summary": "Generator should detect Freshservice-style HTTP-Link-only pagination (no body cursor) and synthesize integer page advancement automatically",
      "reason": "Filed as a printing-press generator issue. Same shape applies to any vendor that paginates via integer ?page=N with the next-page URL exclusively in the Link header."
    },
    {
      "summary": "Generator should learn Basic-auth `<credential>:` (empty password) as a first-class auth shape",
      "reason": "Filed as a printing-press generator issue. Freshservice, Zendesk, and similar vendors use this form; the generator currently emits `<username>:<password>` even when the spec or vendor docs describe an empty-password Basic flow."
    },
    {
      "summary": "Generator should accept lenient subdomain inputs (bare slug, https://, trailing path) for tenant-scoped APIs",
      "reason": "Filed as a printing-press generator issue. normalizeFreshserviceDomain is generic — every tenant-subdomain vendor benefits from the same normalization."
    },
    {
      "summary": "Generator should preserve `private` (and similarly server-default-true) optional bools on the wire instead of omitting on zero value",
      "reason": "Filed as a printing-press generator issue. The omit-zero idiom is correct for most optional bools but inverts intent when the server-side default differs from Go's zero value."
    },
    {
      "summary": "Generator's ClearTokens template should clear API-key fields, not just OAuth fields",
      "reason": "Greptile P1 on PR #533 (Freshservice). The OAuth-shaped ClearTokens applies to every API-key-auth CLI the generator prints — same bug class across the library."
    },
    {
      "summary": "Generator should emit validIdentifierRE guards symmetrically across mirrored store helpers",
      "reason": "Greptile P1 (security) on PR #533. ListField has the guard; ResolveByName, emitted from the same store template, does not. Any CLI generated from this template inherits the asymmetry."
    },
    {
      "summary": "Generator-emitted analytics commands should route writes through cmd.OutOrStdout()",
      "reason": "Greptile P2 on PR #533. The analytics template wires JSON and text output to os.Stdout directly, breaking test redirection and MCP shell-out. Applies to every printed CLI that includes the analytics command."
    },
    {
      "summary": "Generator's auth-status hint should produce env-var-shape-specific placeholders, not a uniform token placeholder",
      "reason": "Greptile P2 on PR #533. The 'your-token-here' placeholder is correct for token env vars but wrong for hosts/domains/region IDs/etc. Every API with a non-token required env var inherits the trap."
    }
  ]
}
